# --- Web framework (Phase 3) ---
fastapi
uvicorn[standard]

# --- Document parsing (Phase 1) ---
pypdf
python-docx

# --- Vector store (Phase 1) ---
chromadb

# --- Embeddings (Phase 1) ---
sentence-transformers
tiktoken

# --- LLM client (Gemini, used for query routing and generation) ---
google-genai

# --- Optional fallback LLM client (see generator_groq_backup.py) ---
# Uncomment if activating the Groq-based generator backup:
# groq

# --- Utilities ---
python-dotenv
pydantic

# --- Testing (not needed for the app itself, only to run tests/) ---
pytest
reportlab