[pytest]
markers =
    integration: marks tests that hit a real external API (costs money/network) - run explicitly with `pytest -m integration`, excluded by default with `pytest -m "not integration"`
