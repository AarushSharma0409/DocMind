VITE_API_BASE_URL=http://localhost:8000
# Optional. Only required if the backend has DOCMIND_API_KEY set — if the
# backend's env var is unset, auth is open and this is ignored either way.
VITE_API_KEY=
